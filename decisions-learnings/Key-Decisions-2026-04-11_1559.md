# Key Decisions & Learnings — 2026-04-11 15:59 UTC (Root Level)

**Session:** Sierra Homes file consolidation + Master-Context.md update
**Model:** Claude Sonnet 4.6 (Anthropic)
**Scope:** AI Memory System + SierraHomes_AmericanDream staging

---

## Decisions

### D-049: SierraHomes staging confirmed as canonical folder
- **Decision:** `projects/staging/SierraHomes_AmericanDream/` is the single canonical folder for all SierraHomes work. `SierraHomes_AmericanDream_AI_Training/` is redundant and ready for deletion by Hassan.
- **Why:** File consolidation now complete (session 7 + this session). All content, agents, session history, and decisions unified in staging.
- **How to apply:** All future SierraHomes sessions open `projects/staging/SierraHomes_AmericanDream/agents/open.md`.

### D-050: Master-Context.md updated with full agent + deliverables index (KD-030)
- **Decision:** Updated `projects/staging/SierraHomes_AmericanDream/Master-Context.md` — Available Agents table expanded (Product_Development, Security, TechSupport added); "Available Deliverables & Content Files" section created indexing all project files by department.
- **Why:** open.md loads Master-Context.md first. Without this index, agents had no visibility of 40+ content files now in the project.
- **How to apply:** Keep Available Deliverables section current as new files are added to the project.

### D-069: Windows CMD git push block — always provide Windows-formatted commands at closure
- **Decision:** At every session close, provide the git push commands formatted for Windows Command Prompt. Never use Linux/Mac syntax (`rm`, forward slashes) in closure commands.
- **Why:** User's machine is Windows (C:\Users\swozz\Documents\AI_Memory_Brain). `rm` is not recognized. Backslashes required for paths.
- **Standard closure block:**
  ```cmd
  cd C:\Users\swozz\Documents\AI_Memory_Brain
  del .git\index.lock
  git add <changed paths with backslashes>
  git commit -m "<description>"
  git push origin main
  ```
- **Notes:** `del .git\index.lock` only needed if the lock file exists. Include it by default — it's harmless if the file doesn't exist (will show a warning, not an error).

## Learnings

### L-030: Sandbox bash clock offset — verify actual date with `date` not `date -u`
- **Issue:** `date -u` in the Cowork sandbox returned 2026-04-08 when actual date was 2026-04-11. Session files initially created with wrong date.
- **Fix:** Confirmed correct date via `date` (local time); renamed both session files during closure.
- **How to apply:** Always run `date` (not `date -u`) at session start before creating timestamped files in Cowork.

---
*Finalized by closure protocol — 2026-04-11.*
